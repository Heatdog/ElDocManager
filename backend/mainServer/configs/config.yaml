
main_server_listen:
  type: port
  bind_ip: localhost
  port: 8080

auth_server_listen:
  type: port
  bind_ip: localhost
  port: 8081

postgre_settings:
  host: postgre
  port: 5432
  database: eldoc
  username: postgres
  password: 123

cors_settings:
  allowed_methods: ["GET", "POST", "DELETE"]
  allowed_headers: ["Content-Type"]
  allowed_origins: ["127.0.0.1:3000"]
  exposed_headers: ["Authorization", "Content-Type"]
